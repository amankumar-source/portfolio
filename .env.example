# Groq API Key (Required for AI chat)
GROQ_API_KEY=your_rotated_groq_api_key

# Configurable Groq Model (Optional, defaults to openai/gpt-oss-20b)
GROQ_MODEL=openai/gpt-oss-20b

# Backend Server Port (Optional, defaults to 5000)
PORT=5000
